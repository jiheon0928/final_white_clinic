export type FormData = {
  name: string;
  phone: string;
  address: string;
  item: string;
  message: string;
  date?: string;
  time?: string;
  price?: number;
};

export type FormDataList = FormData[];
