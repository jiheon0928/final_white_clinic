import { StyleSheet } from "react-native";

export const ReservationDetailStyles = StyleSheet.create({
  box: {
    borderWidth: 1,
    borderColor: "#bbb",
    borderRadius: 16,
    padding: 16,
    backgroundColor: "#fff",
    marginBottom: 24,
    gap: 8,
  },
  requestBox: {
    borderWidth: 1,
    borderColor: "#333",
    borderRadius: 8,
    padding: 12,
    marginVertical: 8,
  },
  label: {
    fontSize: 15,
    marginBottom: 4,
    fontWeight: "bold",
  },
  text: {
    fontSize: 14,
    color: "#333",
  },
  riderInfo: {
    gap: 8,
  },
});
