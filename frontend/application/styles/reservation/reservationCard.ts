import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
  card: {
    gap: 10,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 8,
    padding: 16,
    marginBottom: 10,
  },
});
