import { StyleSheet } from "react-native";

export const checkBoxBundleStyles = StyleSheet.create({
  sectionTitle: {
    fontSize: 15,
    marginLeft: 8,
    marginBottom: 6,
    fontWeight: "bold",
  },
  checkboxRow: {
    padding: 6,
    flexDirection: "row",
  },
});
