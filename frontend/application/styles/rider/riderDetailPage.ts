import { StyleSheet } from "react-native";

export const RiderDetailPageStyles = StyleSheet.create({
  requestBox: {
    borderWidth: 1,
    borderColor: "#333",
    borderRadius: 8,
    padding: 12,
    justifyContent: "center",
  },

  card: {
    borderWidth: 1,
    borderColor: "#888",
    borderRadius: 16,
    padding: 18,
    marginTop: 18,
    gap: 10,
  },
  industryList: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 10,
  },
});
